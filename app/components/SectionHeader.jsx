import React from 'react'

const SectionHeader = ({children}) => {
  return (
    <h2 className='text-center text-neutral-300 text-3xl mb-20 font-bold'>{children}</h2>

  )
}

export default SectionHeader